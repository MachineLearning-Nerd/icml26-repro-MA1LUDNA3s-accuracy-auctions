# Limitations and deviations

This independently instantiates the formal theorem and spans 64x in m; it is not an empirical proof of the theorem for every admissible distribution. The mathematical source audit and stability-band all-user cross-check address that gap. Logistic rather than hinge loss is used, explicitly covered by the theorem.
